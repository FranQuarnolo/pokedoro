export interface PomoSession {
    id: string; // Un UUID o un timestamp
    name: string; // "Estudio"
    durationMinutes: number; // 40
    pokemonId: string; // "pikachu" (en minúsculas, para la URL)
    pokemonNickname: string; // "Juan"
}